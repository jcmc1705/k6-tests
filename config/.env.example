# Copie para .env e preencha. Para o k6 enxergar as variáveis, exporte ao carregar (bash):
#   set -a && source config/.env && set +a && k6 run -e USER=ADM_SENAI ...
# Ou use "export" em cada variável abaixo.

export ENV=preview
export K6_USERNAME=
export K6_PASSWORD=

# Opcional: ID de usuário nos endpoints (testData)
# TEST_USER_ID=6513396


export ADM_SENAI=
export ADM_DR=
export ADM_ESCOLA=
export PASSWORD_PREVIEW=

export ADMSENAI_HML=
export PASSWORD_HML=

# Exemplo por key (exporta as variáveis para o processo k6):
# set -a && source config/.env && set +a && k6 run -e USER=ADM_SENAI tests/load.progress.test.js
#
# Exemplo direto (K6_USERNAME/K6_PASSWORD):
# k6 run -e K6_USERNAME=914.907.050-90 -e K6_PASSWORD="Alm#2020" tests/load.progress.test.js
